# Tracked placeholders for the local `.env/` control plane.
# Copy once:  cp -a env.example .env
# Or run:     ./tool/dev.sh --setup
#
# Never put real release keystores or production passwords here.
# Official package id / update channel live only in your private `.env/`.

See `app.env`, `server.env`, and `android/key.properties.example`.
