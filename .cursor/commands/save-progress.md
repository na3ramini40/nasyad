# Save progress

Append one short line to `docs/domain/ailogs.md`:

```markdown
YYYY-MM-DD HH:MM TZ | <what was done>; <next step if obvious>
```

One line only. Do not commit unless the user also asks to ship.
