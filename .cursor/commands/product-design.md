# Product design

Consult as the **product** skill (`.cursor/skills/shared/product/`) — consult modes only. No app code.

1. Load the skill; read ground-truth shards it lists (app-map, end-user, insights) plus domain structure and scope.
2. Detect mode: **ideate** / **mature** / **audit** (ask once if unclear).
3. Advise per the skill; mature suggestions into a product-ready brief before any `/deliver-feature`.
4. If the session yields a durable bet/gap/rejection, update `insights.md` in the same turn. Screen/persona changes wait for delivery — do not invent them here.
